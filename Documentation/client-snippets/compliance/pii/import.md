```elixir
# No explicit import is required. `pii/1,2` becomes available automatically
# from `use Chronicle.Events.EventType` or `use Chronicle.ReadModels.ReadModel`,
# and `pii/0,1` becomes available automatically from `use Chronicle.Concept`.
```
