```text
Elixir does not support this workflow yet.
Without a single-object nested-projection mechanism, there is no way to
express even one level of nesting, let alone the recursive (nested-within-
nested) case. Track the client SDK issue before relying on nested object
projection from Elixir.
```
