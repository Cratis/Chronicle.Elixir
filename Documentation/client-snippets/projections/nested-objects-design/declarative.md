```text
Elixir does not support this workflow yet.
`Chronicle.Projections.Projection` has no nested single-object mechanism with
clear semantics — only flat `from`/`join` mappings and list-style children via
`:parent_key`. Track the client SDK issue before relying on nested object
projection from Elixir.
```
